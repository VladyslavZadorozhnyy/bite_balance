<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:parentTag="androidx.constraintlayout.widget.ConstraintLayout">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="2dp">

        <View
            android:id="@+id/form_stroke"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingTop="10dp" />

        <EditText
            android:id="@+id/form"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:singleLine="true"
            android:imeOptions="actionDone"
            android:paddingTop="10dp"
            android:paddingLeft="20dp"
            android:layout_margin="2dp"
            android:textCursorDrawable="@null"
            app:layout_constraintStart_toStartOf="@id/form_stroke"
            app:layout_constraintEnd_toEndOf="@id/form_stroke"
            app:layout_constraintTop_toTopOf="@id/form_stroke"
            app:layout_constraintBottom_toBottomOf="@id/form_stroke" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</merge>